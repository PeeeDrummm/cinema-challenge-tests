name: CI/CD - Cinema Challenge Tests

on:
  push:
    branches:
      - develop

# Permissões necessárias para que a Action possa criar um PR
permissions:
  contents: write
  pull-requests: write

jobs:
  run-tests:
    name: Run Robot Framework Tests
    runs-on: ubuntu-22.04

    env:
      MONGO_URI: ${{ secrets.MONGO_URI }}
      # JWT_SECRET: ${{ secrets.JWT_SECRET }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install Python dependencies
        working-directory: ./cinema-automation
        run: pip install -r requirements.txt

      - name: Install Playwright system dependencies
        run: python -m playwright install-deps

      - name: Install Playwright browsers
        run: python -m playwright install

      - name: Run Robot Framework tests
        working-directory: ./cinema-automation
        run: robot --outputdir logs tests/

  create-pr:
    name: Create Pull Request to main
    # Garante que este job só rode se 'run-tests' for concluído com sucesso
    needs: run-tests
    runs-on: ubuntu-latest

    steps:
      # A CORREÇÃO: Adicionamos este passo para baixar o código no ambiente do job.
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Create Pull Request if one does not exist
        env:
          # O GITHUB_TOKEN é um segredo especial que o GitHub fornece automaticamente
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # Uma forma mais robusta de verificar se já existe um PR aberto
          EXISTING_PR=$(gh pr list --head develop --base main --state open --json number -q '.[0].number')
          
          if [ -z "$EXISTING_PR" ]; then
            echo "No open PR found from develop to main. Creating a new one."
            gh pr create \
              --base main \
              --head develop \
              --title "🔁 [Automated] Merge develop into main after successful tests" \
              --body "✅ Testes automatizados passaram com sucesso. PR gerado automaticamente."
          else
            echo "Pull Request #$EXISTING_PR from develop to main already exists. Nothing to do."
          fi
