# Core do Robot Framework
robotframework==6.1.1

# Bibliotecas padrão usadas nos testes
robotframework-requests==0.9.5
robotframework-dotenv==0.4.0
robotframework-datadriver==1.8.1  # (opcional se usar testes com dados em CSV/Excel)

# Suporte a MongoDB com Python
pymongo==4.6.1
bcrypt==4.1.2

# Carregamento de .env para uso via os.getenv()
python-dotenv==1.0.1
